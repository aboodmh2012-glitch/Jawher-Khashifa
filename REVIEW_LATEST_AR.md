# مراجعة موحّدة — آخر تحديثات Travel (آب 2026)

مراجعة مقارنة بعد سحب آخر الفروع من المستودع ودمجها في شجرة المراجعة:

| الفرع | المحتوى | الإصدار |
|-------|---------|---------|
| `claude/new-session-nlwwva` | أساس `trip` | 1.8.0 |
| `claude/fusion-travel-repo-g7arg4` | إصلاحات أمنية على `trip` | **1.8.1** |
| `claude/fusion-travel-repo-p1eir5` | إصلاحات على `trip` (غير مدمجة في 1.8.1) | 1.7.2 |
| `claude/fusion-travel-repo-zstcx3` | موديول `fusion_travel` الموحّد لـ Odoo 19 | **19.0.2.4.0** |

---

## الحكم السريع

1. **`trip` 1.8.1:** أصلح ثغرتين حرجتين من مراجعتنا السابقة (تسريب البوابة + تلاعب سعر التحويل)، لكن **خصم/حجز مكرر ما زال مفتوحاً** → غير جاهز للإنتاج العام.
2. **`fusion_travel` 19.0.2.4.0:** أقوى بكثير أمنياً ومحاسبياً، وهو **المسار الموصى به** لـ Odoo 19 — لكن ما زال يحتاج Staging حيّ + إصلاحات High قبل الإطلاق.
3. **لا تثبّت `trip` و`fusion_travel` معاً** في نفس قاعدة البيانات (تحذير README).

---

## أ) حالة `trip` بعد 1.8.1

### ما أُغلق من المراجعة السابقة

| البند | الحالة في 1.8.1 | الدليل |
|-------|-----------------|--------|
| C1 تسريب `trip.operation` للبوابة | **FIXED** | `trip_operation_portal_own_rule` في `trip_security.xml` |
| C3 تلاعب سعر التحويلات عبر المتصفح | **FIXED** | HMAC-SHA256 على `_payload` في `website_trip.py` |
| C2 دفع/حجز مكرر (لا idempotency) | **ما زال مفتوحاً** | `action_create_provider_order` بلا حارس؛ `/issue` يعيد الاستدعاء |

### فجوة مهمة: إصلاحات 1.7.2 لم تُدمَج في 1.8.1

فرع `p1eir5` (1.7.2) فيه إصلاحات **غير موجودة** في 1.8.1:

- تخطي `action_create_provider_order` إذا وُجد `amadeus_reference` أو حالة `booked/ticketed/confirmed`
- فرض تاريخ ميلاد للطفل/الرضيع قبل Create Orders
- رفع `custom_app_version` إلى 1.7.2

أي أن 1.8.1 أصلح C1+C3 فقط، بينما 1.7.2 أصلح C2+جزء من H2 على قاعدة أقدم — **لم يُدمجا**.

### ما زال مفتوحاً في trip 1.8.1 (High)

- انتقالات حالة حرّة + Void/Refund بلا عكس مالي
- DOB/هاتف وهميان + غياب `travelerType`
- SO/فاتورة بدون `product_id`
- مسار «بطاقة Amadeus» stub
- لا اختبارات آلية
- توقيع العرض بلا TTL (إعادة استخدام عرض قديم موقَّع)

**حكم trip:** تشغيل داخلي فقط حتى يُدمَج حارس C2 على الأقل.

---

## ب) حالة `fusion_travel` 19.0.2.4.0

### ما أصلحه مقارنة بـ `trip`

| قضية trip القديمة | في fusion_travel |
|-------------------|------------------|
| تسريب portal operations | لا يوجد نموذج operation مكشوف؛ كل نماذج البوابة لها `ir.rule` |
| تلاعب عرض المتصفح | عروض على الخادم + توكنات opaque لمرة واحدة (أقوى من HMAC) |
| خصم محفظة مزدوج | `FOR UPDATE` + advisory lock + `idempotency_key` فريد |
| DOB/هاتف وهميان | مرفوضان صراحة (لكن التحقق للطيران متأخر — بعد الدفع) |
| SO/فاتورة بلا منتج | `_get_service_product()` + منتجات XML افتراضية |
| SDK مؤرشف | عميل REST مباشر + OAuth في الذاكرة |
| محاسبة ضعيفة | تذاكر + تسوية محفظة حقيقية + أدوار Accountant |

التفاصيل موثّقة أيضاً في `fusion_travel/docs/DEEP_REVIEW_AR.md` وتم التحقق منها مقابل الكود.

### مشاكل High ما زالت / فاتت المراجعة السابقة

1. **رضيع بلا `associatedAdultId`** — موجود في `trip`، محذوف هنا → فشل Amadeus محتمل بعد خصم المحفظة.
2. **تسعير فندق:** إذا لم يُطابق `offer_id` يُؤخذ `offers[0]` → سعر خاطئ محتمل.
3. **تحقق مسافر الطيران (DOB/هاتف) بعد الدفع** — المحفظة تُخصم ثم يفشل Create Orders.
4. طلب المورد داخل معاملة Odoo (نجاح خارجي + فشل commit نادر).
5. لا Registry/Migration/Amadeus حي على Staging بعد.
6. البطاقة المستضافة معطّلة حتى Adapter PCI.
7. Rate limit عام يحتاج WAF/Proxy.

### ما هو متين

حماية RPC للحقول الحساسة، أدوار User/Accountant/Manager، عروض قصيرة العمر، تسوية محفظة محاسبية، تنقيح أسرار API، قواعد شركات، بوابة read-only بملكية الشريك.

---

## ج) أي مسار تعتمد؟

| السيناريو | التوصية |
|-----------|---------|
| Odoo 19 + ترقية من Fusion Travel الحالي | **`fusion_travel` 19.0.2.4.0** (PR #1) |
| الاستمرار على موديول `trip` المنفصل | ادمج 1.8.1 + إصلاحات C2/DOB من 1.7.2 قبل أي بوابة عامة |
| تشغيل داخلي سريع | أيهما، مع إيقاف المسارات العامة الحساسة |

**التوصية النهائية:** اعتمد `fusion_travel` كمسار إنتاج، وأصلح البنود High 1–3 أعلاه قبل الإطلاق؛ استخدم Staging كامل كبوابة اعتماد (موجودة في DEEP_REVIEW).

---

## د) ترتيب عمل مقترح

### إن اخترت fusion_travel (موصى)
1. إعادة `associatedAdultId` للرضّع + إرسال `travelerType`
2. التحقق من DOB/هاتف **قبل** خصم المحفظة
3. رفض تسعير فندق إذا لم يُطابق `offer_id` (لا تسقط على `offers[0]`)
4. Staging: upgrade + أدوار + محفظة + Amadeus test + `needs_review`
5. WAF rate limit + Adapter بطاقة عند الحاجة

### إن أبقيت trip
1. نقل حارس idempotency من 1.7.2 إلى 1.8.1
2. فرض DOB للطفل/الرضيع
3. تعطيل مسار البطاقة الوهمي من الموقع
4. منتجات SO/فاتورة
5. ثم فقط فكّر بالبوابة العامة

---

## تحقق هذه المراجعة

- سُحبت آخر الفروع: `g7arg4` (trip 1.8.1) + `zstcx3` (fusion_travel 19.0.2.4.0)
- قورنت مع `p1eir5` (trip 1.7.2) لاكتشاف فجوة الدمج
- تُحقّق من ادّعاءات `DEEP_REVIEW_AR.md` مقابل الكود الفعلي
